# Base URL of the backend API.
# Leave empty when running locally (Vite's dev proxy handles /api/* → localhost:3001).
# Set to the full backend origin when deploying the frontend separately, e.g.:
# VITE_API_BASE_URL=https://obiadek-backend.vercel.app
VITE_API_BASE_URL=
